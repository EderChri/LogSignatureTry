[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mvcl"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
